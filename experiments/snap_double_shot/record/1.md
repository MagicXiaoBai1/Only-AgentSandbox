# 1) 清泄漏的 bake firecracker + 残留 jail root + 旧 netns
sudo pkill -f 'firecracker --id bake-base-1'
sudo rm -rf /home/yunfei/Code/Only-AgentSandbox/tmp/oas-bake/firecracker/bake-base-1
sudo rm -rf /home/yunfei/oas-test/firecracker/sb-*
for ns in $(ip netns list | awk '/^oas-sb-|^bake-/{print $1}'); do sudo ip netns del "$ns"; done

# 2) 起 runtime，带日志（模块级 debug）
cd /home/yunfei/Code/Only-AgentSandbox/only-agent-sandbox
sudo OAS_SOCKET=/run/oas.sock \
     RUST_LOG="oas_runtime=debug,oas_driver=debug,oas_manager=debug,oas_shim=debug,info" \
     OAS_LOG_FILE=/home/yunfei/Code/Only-AgentSandbox/tests/agent-rt-test/logs/oas.log \
     ./target/debug/oas-runtime --config /etc/oas/config.toml &

# 3) 起 standalone kubelet
cd /home/yunfei/Code/Only-AgentSandbox/tests && bash start-kubelet.sh

# 4) 投静态 Pod 触发（或等已有 yaml 被 sync）